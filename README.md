# basic-assembler-runtime
This code includes a basic assembler that converts the assembly code to hex instructions. Then there is a runtime that will parse these instructions and emulate a processor.
